{
  "name": "y-tools",
  "version": "0.0.0",
  "author": "yued-fe.TM",
  "description": "Sketch plugin tools yued-fe.TM",
  "repository": "git@github.com:yued-fe/y-tools.git",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "y-tools",
    "manifest": "src/manifest.json",
    "main": "y-tools.sketchplugin"
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.2"
  }
}
